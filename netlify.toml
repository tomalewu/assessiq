[build]
  command = "npm install && npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions.generate]
  timeout = 30

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
